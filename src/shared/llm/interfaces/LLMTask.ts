/**
 * Supported LLM task types in Sequenzia.
 * Defines what kind of structured outputs we expect from LLMs.
 */
export type LLMTask = 'remi' | 'chords' | 'extraction' | 'summarization'; // Add more as needed
